import { OpenProfileDirective } from './open-profile.directive';

describe('OpenProfileDirective', () => {
  it('should create an instance', () => {
    const directive = new OpenProfileDirective();
    expect(directive).toBeTruthy();
  });
});
