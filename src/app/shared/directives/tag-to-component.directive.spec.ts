import { TagToComponentDirective } from './tag-to-component.directive';

describe('TagToComponentDirective', () => {
  it('should create an instance', () => {
    const directive = new TagToComponentDirective();
    expect(directive).toBeTruthy();
  });
});
