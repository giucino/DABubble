import { PasteAsTextDirective } from './paste-as-text.directive';

describe('PasteAsTextDirective', () => {
  it('should create an instance', () => {
    const directive = new PasteAsTextDirective();
    expect(directive).toBeTruthy();
  });
});
